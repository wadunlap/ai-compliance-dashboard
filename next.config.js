/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  // Remove the swcMinify option as it's no longer needed in Next.js 15
  };
  
  module.exports = nextConfig;
  